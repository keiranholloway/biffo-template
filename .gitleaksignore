# Fingerprints of specific findings accepted as known-safe, NOT a weakening
# of the rule or a general regex/value allowlist entry. Each line names one
# finding at one exact historical commit; see the comment beside it for why.
#
# `.gitleaks.toml`'s `biffo-aws-account-id` rule (#1628, third and current
# attempt) matches `\b(?:[0-9A-Fa-f]{4}-[0-9A-Fa-f]{4}-)?(\d{12})\b`, reports
# only the 12 digits via `secretGroup`, and exempts that finding only when
# the RAW MATCH (not the reported secret) is a complete two-hex-quad-prefixed
# shape (`regexTarget = "match"` on the allowlist). AGENTS.md's own prose,
# written while explaining the rule's history (#893), used an abbreviated
# form -- an ellipsis, a hyphen, and a bare 12-digit run -- to illustrate
# "the last segment of a UUID", rather than spelling out two complete hex
# quads. That abbreviated form does not match the two-hex-quad prefix, so the
# allowlist does not exempt it, and it is frozen forever in the commit that
# first wrote it (#893's own commit, already on `dev` before this rule
# change, not introduced by this branch). Editing AGENTS.md at any later
# commit cannot clear a git-history-wide scan of an earlier one -- exactly
# the point AGENTS.md §7 itself makes about this same rule. Confirmed via
# `git merge-base --is-ancestor` that the commit is already an ancestor of
# `origin/dev`, so this is pre-existing repo history surfaced by broadening
# the regex, not something this branch introduced. Re-confirmed live for the
# third attempt's regex too: removing this file surfaces exactly the one
# finding below, at the same commit/path/line.
#
# The live AGENTS.md text has already been rewritten (this same PR) to spell
# out a complete UUID's two-hex-quad shape instead, so this ignore entry only
# ever applies to the frozen historical commit, never to anything written
# from here on.
5ee467041cf0e6acf72bac43b018243a25a37719:AGENTS.md:biffo-aws-account-id:482
