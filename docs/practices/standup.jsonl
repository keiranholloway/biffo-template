{"date": "2026-07-30", "chosenAt": "2026-07-30T07:53:06.395Z", "kind": "redBranch", "label": "Integration branch red \u2014 everyone blocked at once", "metric": "windows.1.repos[\"keiranholloway/biffo-plugin-ideation\"].integration.redMinutes", "metricValue": 1268.3, "costMinutes": 1268.3, "note": "make integrationHealth.redMinutes blocking-aware: stop counting idle time (18.8h of ideation's 21.1h was overnight with zero pushes)"}
{"date": "2026-07-30", "chosenAt": "2026-07-30T10:25:00.000Z", "kind": "redBranch", "label": "Integration branch red \u2014 everyone blocked at once (AMENDED)", "metric": "windows.1.repos[\"keiranholloway/biffo-plugin-ideation\"].integration.redMinutes", "metricValue": 1268.3, "costMinutes": 1268.3, "note": "AMENDS the 07:53 entry, which recorded the baseline before I understood what tomorrow would read. The fix SHIPPED (#921). Tomorrow this metric will read ~200 instead of 1268.3 and the loop will say 'improved' \u2014 but that is a DEFINITION CHANGE, not a behaviour change: redMinutes now caps idle gaps at 60min. Do NOT bank it as a win. The honest first data point on whether this process compounds is 2026-07-31, on a metric nobody redefined. Also shipped today: 8 further PRs from the backlog triage, and #648 items 2+3 deliberately NOT built (would report a version 140 releases behind)."}
{"date":"2026-07-30","chosenAt":"2026-07-30T19:44:45.121Z","kind":"directed","label":"close the remaining #956 Group A fail-opens (.githooks not armed, #668 empty-router guard, strings/grep over a compressed artefact)","metric":null,"metricValue":null,"costMinutes":null,"note":"validate each is still real BEFORE building — four of today's findings were stale items or wrong estimates. No snapshot metric tracks unfixed fail-opens; the corpus count is the real measure and is not collected. Candidate follow-up: collect it."}
{"date":"2026-07-31","chosenAt":"2026-07-31T04:22:47.257Z","kind":"contention","label":"Merge contention — correct work that could not land","metric":"windows.1.repos[\"tabsii-com/tabsii-platform\"].contention.greenButUnmergedHours","metricValue":16.8,"costMinutes":1008,"note":"Extend H3's strict:false to tabsii-platform dev (biggest cost carrier, 50.8% of estate contention), and close line 144's open gap by collecting a stale-merge counter-metric (head behind base at merge) so the relaxation is falsifiable on content loss, not just merge friction. tabsii-crm stays untouched as H3's comparator."}
{"date":"2026-07-31","chosenAt":"2026-07-31T15:07:54.863Z","kind":"directed","label":"Make RLS invisibility structurally unshippable: a test session that is genuinely RLS-constrained, rowcount assertion on RLS-gated writes, NULL-scope policy audit","metric":null,"metricValue":null,"costMinutes":null,"note":"Mechanism: (1) shared pytest fixture binding get_db to a session that is NOT the BYPASSRLS schema owner both lanes share today, so a permission-less read raises rather than returning zero rows; (2) assert rowcount on RLS-gated writes so an RLS-refused UPDATE cannot report success from intent (#373 did one endpoint, #372's audit of 22 mid-request commits is open); (3) audit NULL scope args in db/imports/tabsii/011_rls_policies.sql, where a NULL-scope fn_authorized call is only satisfiable by a tenant-wide assignment so brand-scoped roles silently read/write nothing. No snapshot metric tracks this; the real measure is the count of RLS-gated reads/writes with no visibility guard, baselined by hand today. Baseline: 3 instances shipped green in one day (media_assets, audit_logs, users), both test lanes structurally blind."}
{"date":"2026-08-01","chosenAt":"2026-08-01T07:06:52.709Z","kind":"directed","label":"Make branch protection's advisory-ness visible: extend the daily protection audit to report enforce_admins, and file the unfiled fail-open","metric":"failOpenBacklog.unfiled","metricValue":11,"costMinutes":null,"note":"development-practices.md:260 is unfiled and not fixed: enforce_admins=false on 11 of 12 repos makes every protection rule advisory for the only human who merges, while the daily audit prints 'OK protection - 19 branches checked, all protected' every day. Verified live: biffo-template true, tabsii-platform FALSE. Mechanism: (1) extend scripts/protection-audit.sh to fetch and report enforce_admins per protected branch alongside the existing protected check, and FAIL (not warn) when protection does not bind admins, so the false green becomes a real signal; (2) file the fail-open as a GitHub issue so failOpenBacklog.unfiled goes 11 -> 10. Deliberately reports rather than changes enforce_admins/strict: H3 reviews 2026-08-04 and altering either setting would corrupt its arms, whereas reporting makes the confound (comparator tabsii-crm is unbound) visible before the review rather than after. NOT taking rank 2: its 273 red minutes had two template-owned causes (80.2MB inline Lambda cap, python-not-found exit 127) both already fixed and distributed via core 0.204.1 this morning, dev green at 06:36."}
{"date":"2026-08-02","chosenAt":"2026-08-02T04:12:56.577Z","kind":"directed","label":"restore the metrics collector: GitHub's node-limit now rejects every bulk PR fetch","metric":null,"metricValue":null,"costMinutes":null,"note":"GitHub tightened the GraphQL node budget server-side; gh pr list --json commits is now a static 100 PRs x 100 commits x 100 authors = 1,000,000 nodes > the 500,000 max, so it fails at ANY --limit (verified 1000/500/250/100 all identical) on all 15 repos. gh 2.96.0 unchanged since Jul 2, and the same query works interactively minus 'commits', so this is a GitHub-side change, NOT the credentials the FATAL message blames. Preflight (#917) correctly refused to write an empty snapshot, so 2026-08-02 has NO data at all and yesterday's enforce_admins choice cannot be closed. Mechanism: drop 'commits' from the bulk fetchPrs field list, and lazily fetch it per-PR via 'gh pr view <n> --json commits' (single PR = 10,000 nodes, verified working) ONLY for upgrade PRs whose body-based parseCarriedPrs found nothing - the one call site, line 355, is already gated by isUpgradePr, so the fallback set is a handful of PRs rather than O(all). Also fix the misleading FATAL text to stop asserting credentials when the failure is a GraphQL node limit."}
